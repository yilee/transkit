{
  "name": "@transkit/chrome-extension",
  "version": "0.1.0",
  "description": "Chrome extension for transkit translation tool",
  "private": true,
  "scripts": {
    "build": "tsc && node build.mjs",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
